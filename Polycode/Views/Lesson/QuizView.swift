//
//  QuizView.swift
//  Polycode
//
//  container that handles a single quiz step
//

import SwiftUI

/*
 struct QuizView: View {
 let step: QuizStep
 
 var body: some View {
 VStack {
 Text(step.prompt).font(.title2)
 switch step.type {
 case .multipleChoice(let options, let correct):
 MCView(options: options, correctIndex: correct)
 case .tokenReorder(let tokens, let correctOrder):
 DnDView(tokens: tokens, correctOrder: correctOrder)
 case .codeFix(let initial, let expected):
 CodeFixView(initialCode: initial, expectedCode: expected)
 case .codeWrite(let prompt, let expected):
 CodeWriteView(prompt: prompt, expectedOutput: expected)
 }
 }
 }
 }
 */
